# Node
node_modules/

# Local env
.dev.vars
web/.env.local

# Ignore build output and stray routes
web/dist/
web/dist/_routes.json

# Ignore build output
web/dist/
# keep lockfile for reliable Netlify builds
# (do NOT ignore package-lock.json)
