export default function Upload() {
  return (
    <section className="nv-section">
      <h2 className="nv-h2">Upload your own</h2>
      <p className="nv-muted">Next PR will add dropzone + preview + save. For now this is a placeholder so the hub structure is complete.</p>
    </section>
  );
}
