export type Navatar = {
  id: string;
  name?: string;
  imageUrl?: string;      // for public assets
  imageDataUrl?: string;  // for uploads
  species?: string;
  base?: string;
  createdAt: number;
};
