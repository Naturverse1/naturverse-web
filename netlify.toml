[build]
  base = "web"          # run builds inside /web
  publish = "dist"      # publish the dist *inside* /web
  functions = "functions"
  command = "npm ci && npm run build"

# keep the SPA redirect
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
