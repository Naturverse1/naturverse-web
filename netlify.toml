[build]
  command = "npm run build"
  publish = "dist"
  node_version = "20"

[build.environment]
  NODE_VERSION = "20"
# Vite env vars are provided in Netlify UI as VITE_* (do NOT hardcode secrets here)
