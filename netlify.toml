[build]
base = "web"
publish = "dist"
command = "npm install --include=dev --no-audit --no-fund && npm run build"

[functions]
directory = "web/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
