[build]
  base = "web"
  command = "npm install --no-audit --no-fund && npm run build"
  publish = "web/dist"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
