[build]
base = "web"
command = "npm ci --no-audit --no-fund && npm run build"
publish = "dist"
functions = "functions"

[build.environment]
NODE_VERSION = "20"
NPM_FLAGS = "--no-audit --no-fund"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
