[build]
  base = "web"
  command = "npm install --no-audit --no-fund && npm run build"
  publish = "dist"

[functions]
  directory = "functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
