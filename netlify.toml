[build]
  base = "."
  command = "npm install --no-audit --no-fund && npm run build"
  publish = "dist"        # ✅ only dist

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
