[build]
  base = "web"
  command = "npm ci --no-audit --no-fund && npm run build"
  publish = "dist"

[functions]
  directory = "web/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
