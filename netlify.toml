[build]
  command   = "npm ci && npm run build"
  publish   = "web/dist"
  functions = "web/functions"

[functions]
  node_bundler = "esbuild"

# keep the SPA redirect
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
